<Window x:Class="PROJECT.CreateExerciseDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PROJECT"
        mc:Ignorable="d"
        Title="Создание карточки" Height="300" Width="450">
    <!--Чтобы не добавлять по отдельности -->
    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
        </Style>
        <Style x:Key="TextStyle_TextBlock" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
        </Style>
    </Window.Resources>
    
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Название:" FontWeight="Bold"/>
        <TextBox x:Name="NameBox" Grid.Row="1" Margin="0,5"/>

        <TextBlock Text="Описание:" FontWeight="Bold" Grid.Row="2"/>
        <TextBox x:Name="DescriptionBox" Grid.Row="3" Margin="0,5" AcceptsReturn="True" Height="60"/>

        <StackPanel Orientation="Horizontal" Grid.Row="4" Margin="0,10,0,0">
            <TextBlock Text="Повторений:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBox x:Name="RepsBox" Width="60" Text="10"/>

            <TextBlock Text="Подходов:" VerticalAlignment="Center" Margin="20,0,10,0"/>
            <TextBox x:Name="SetsBox" Width="60" Text="3"/>
            <TextBlock Text="Вес" VerticalAlignment="Center" Margin="20,0,10,0"/>
            <TextBox x:Name="WeightBox" VerticalAlignment="Center" Width="30" Text="25"/>
            <TextBlock Text=" кг"/>


        </StackPanel>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Grid.Row="5" Height="30">
            <Button Content="Отмена" Width="80" Margin="0,0,10,0" Click="Cancel_Click" Style="{StaticResource ButtonStyle}"/>
            <Button Content="Создать" Width="80" Click="Create_Click" IsDefault="True" Style="{StaticResource ButtonStyle}"/>
        </StackPanel>
        
    </Grid>
</Window>
